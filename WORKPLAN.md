# Scent Space Simulator 작업 정리

## 1. 현재 상황

- 원래 작업해야 할 대상은 발밤 프로젝트가 아니라 별도 웹사이트입니다.
- 제가 실수로 `C:\Users\admin\Projects\balbam-ai-code` 안의 홈 화면을 향수 팝업 사이트로 교체했습니다.
- 발밤 프로젝트가 깨지지 않도록 제가 바꾼 파일은 원복했고, 참고 산출물은 별도 폴더로 옮겼습니다.

## 2. 옮긴 위치

새 작업 위치:

`C:\Users\admin\Projects\scent-space-simulator`

옮긴/보존한 파일:

- `legacy-balbam-prototype/Home.from-balbam.tsx`
- `legacy-balbam-prototype/App.from-balbam.tsx`
- `reference-artifacts/ref-index.js`
- `reference-artifacts/ref-index.css`
- `reference-artifacts/scent-*.png`

발밤 프로젝트에서 원복한 파일:

- `C:\Users\admin\Projects\balbam-ai-code\client\src\App.tsx`
- `C:\Users\admin\Projects\balbam-ai-code\client\src\pages\Home.tsx`

발밤 프로젝트에 남아 있는 변경:

- `.agents/skills/gstack`
- `package.json`
- `desktop/`

위 3개는 제가 이번 향수 사이트 작업에서 만든 변경이 아니므로 건드리지 않았습니다.

## 3. 레퍼런스 방향

전체 디자인 톤:

- `https://scent-space-guide--msuu0107.replit.app/`의 감성적인 Scent Space 디자인을 기본으로 사용합니다.
- 정적인 안내 페이지가 아니라, 향수 팝업을 준비하는 사람이 실제로 공간을 이해하고 실험할 수 있는 전문 웹앱으로 만듭니다.

기능 방향:

- `https://8acdd840-7b73-40b1-89ba-ab3b604a46e9-00-2o7y7yqazj238.pike.replit.dev/`처럼 공간 시뮬레이션, 내부 보기, 가이드, 예산 정보를 함께 제공합니다.
- 360도 파노라마에 가까운 내부 보기 또는 드래그 가능한 내부 공간 뷰를 제공합니다.
- 내부 공간의 요소를 클릭하면 향의 흐름, 공간 역할, 예산 범위를 확인할 수 있게 합니다.
- 사용자가 직접 조작 가능한 그리드 기반 향 흐름 시뮬레이터를 만듭니다.

## 4. 잘못되면 안 되는 부분

- 향이 벽을 뚫고 지나가면 안 됩니다.
- 도면의 검은 선은 벽 또는 고정 구조물로 취급합니다.
- 향 확산 시뮬레이션은 벽 충돌을 고려해야 합니다.
- 가벽, 서큘레이터, 온열/냉각 장치는 사용자가 마우스로 직접 배치할 수 있어야 합니다.
- 각 단계에서 사용할 수 있는 도구가 제한되어야 합니다.
  - 1단계: 가벽만
  - 2단계: 서큘레이터만
  - 3단계: 온도조절만
  - 4단계: 모든 요소
- 출발점과 도착점도 사용자가 직접 설정할 수 있어야 합니다.

## 5. 제작 순서

### 1단계. 할 일 정하기

- 사이트 구조 확정:
  - 홈
  - 거리/외부 진입
  - 내부 공간 보기
  - 공간디자인 가이드
  - 향 흐름 시뮬레이터
- 구현 범위 확정:
  - 정적 랜딩 페이지가 아니라 실제 조작 가능한 웹앱으로 제작
  - 도면 기반 시뮬레이션이 핵심

### 2단계. 도면 파악하기

- 제공된 도면을 기준으로 벽, 출입구, 빈 공간을 분리합니다.
- 검은 선은 통과 불가능한 벽으로 모델링합니다.
- 곡선 벽도 충돌 영역으로 반영합니다.
- 도면을 SVG 또는 Canvas 좌표계로 옮긴 뒤, 시뮬레이션용 격자 데이터로 변환합니다.
- 향의 시작점과 도착점 후보 위치를 도면 내부의 이동 가능한 공간에만 둘 수 있게 합니다.

### 3단계. 시뮬레이션 모델 만들기

- 단순히 선을 그리는 방식은 사용하지 않습니다.
- 격자 기반 확산 또는 입자 기반 확산을 사용합니다.
- 벽 셀은 통과 불가로 처리합니다.
- 서큘레이터는 특정 방향으로 향 입자 또는 농도장을 밀어냅니다.
- 온열 장치는 확산 속도를 높이고, 냉각 구역은 확산을 느리게 하거나 잔향을 오래 남기게 합니다.
- 가벽은 사용자가 배치하는 임시 장애물로 처리합니다.

### 4단계. UI 제작

- 전체 디자인은 원본 Scent Space의 감성적인 톤을 유지합니다.
- pike 버전처럼 네비게이션을 둡니다.
  - 거리
  - 360도 내부
  - 가이드
  - 시뮬레이션
- 내부 보기에서는 요소를 클릭해 설명 패널을 엽니다.
- 시뮬레이터에서는 도구 팔레트, 단계 선택, 시작/목표 설정, 실행/초기화 버튼을 제공합니다.

### 5단계. 검증

- 데스크톱과 모바일에서 텍스트가 겹치지 않는지 확인합니다.
- 향 입자가 벽을 통과하지 않는지 확인합니다.
- 가벽/장치 드래그 배치가 정상 동작하는지 확인합니다.
- 각 단계에서 허용되지 않은 도구가 배치되지 않는지 확인합니다.
- 실제 브라우저 스크린샷으로 화면을 검수합니다.

## 6. 다음 작업에서 만들 파일 예상

아직 제작은 시작하지 않았습니다. 다음 단계에서 새 프로젝트 안에 아래 형태로 구성하는 것이 좋습니다.

- `package.json`
- `index.html`
- `src/main.tsx`
- `src/App.tsx`
- `src/styles.css`
- `src/data/floorplan.ts`
- `src/lib/simulation.ts`
- `src/components/FloorPlanSimulator.tsx`
- `src/components/InteriorExplorer.tsx`
- `src/components/SpaceGuide.tsx`

## 7. 핵심 구현 메모

- 시뮬레이션은 SVG 장식이 아니라 실제 도면 좌표를 기준으로 합니다.
- 벽을 통과하지 않는 확산을 위해 BFS/격자 확산 또는 입자-충돌 방식 중 하나를 선택합니다.
- 정확성과 구현 안정성을 생각하면 1차 버전은 격자 확산이 좋습니다.
- 사용자 배치 요소가 많아지면 Canvas 기반으로 바꾸는 것이 성능에 유리합니다.
- 도면은 먼저 SVG로 사람이 보기 좋게 그리고, 내부적으로는 2D grid mask를 따로 둡니다.

## 8. 2026-05-04 진행 내용

가장 어려운 부분부터 작업했습니다.

- 의존성 없이 실행 가능한 정적 웹앱 구조를 만들었습니다.
- `index.html`, `src/styles.css`, `src/app.js`, `src/floorplan.js`, `src/simulation.js`를 추가했습니다.
- 제공된 도면을 기준으로 고정 벽, 내부 벽, 곡선 벽을 좌표 데이터로 옮겼습니다.
- 벽/가벽을 통과하지 않는 격자 기반 향 확산 엔진을 만들었습니다.
- 가벽, 서큘레이터, 온열, 냉각 장치를 도면 위에 배치하고 드래그할 수 있게 했습니다.
- 출발점과 도착점도 도면 위에서 직접 이동할 수 있게 했습니다.
- 단계별 도구 제한을 넣었습니다.
  - 1단계: 가벽
  - 2단계: 서큘레이터
  - 3단계: 온열/냉각
  - 4단계: 전체 도구
- 간이 360도 내부 보기와 요소 정보 패널을 추가했습니다.
- Node smoke test로 아래 항목을 확인했습니다.
  - 열린 공간은 통과 가능
  - 고정 벽은 통과 불가
  - 외부 영역은 통과 불가
  - 사용자가 둔 가벽은 동적 벽으로 처리
  - 연결된 공간에서는 향 농도가 도착점까지 확산

실행 URL:

`http://127.0.0.1:4173/`

## 9. 2026-05-04 추가 수정

사용자 피드백:

- 현재 사이트 도면이 원본 1번 이미지가 아니라 2번 이미지처럼 보입니다.
- 곡선 벽이 이상하고, 도면 기반 360도 뷰어도 필요합니다.
- 메인 화면, 360도 뷰어, 시뮬레이션을 각각 페이지로 분리해야 합니다.

반영 내용:

- `FLOORPLAN_SPEC.md`를 추가했습니다.
- 원본 도면 좌표를 별도 문서로 명세했습니다.
- `src/floorplan.js`를 원본 도면 기준으로 교정했습니다.
- 좌측 곡선 벽을 3중 동심원이 아니라 `굵은 주 곡선 벽 + 짧은 곡선 조각`으로 변경했습니다.
- 우측 하단 방 입구를 막던 잘못된 세로벽 해석을 제거했습니다.
- 하단 블록은 별도 `wallBlocks`로 관리합니다.
- `getWallLines()`를 추가해 시뮬레이션과 360도 뷰어가 같은 도면 데이터를 쓰게 했습니다.
- 메인/360도 뷰어/시뮬레이션을 해시 라우팅 페이지로 분리했습니다.
- 360도 뷰어는 도면의 벽 선분을 raycast해서 내부 벽면을 그립니다.
- 360도 뷰어에 현재 시점 미니맵과 요소 설명 패널을 추가했습니다.

검증:

- `node --check src/floorplan.js`
- `node --check src/app.js`
- `node --check src/simulation.js`
- Node smoke test:
  - 상단 좌측 외부 영역 차단
  - 메인 공간 통과 가능
  - 중앙 내부 벽 차단
  - 우측 하단 방 통과 가능
  - 우측 긴 벽 차단
  - 곡선 벽 차단
  - 동적 가벽 차단
  - 도면 벽 선분이 360도 뷰어에 제공됨
  - 연결된 공간에서 향 농도 도착점 확산 확인

브라우저 캡처:

- `reference-artifacts/page-home.png`
- `reference-artifacts/page-viewer.png`
- `reference-artifacts/page-simulator.png`

## 다음 AI CLI 작업 결과

- 다중 출발점 구현 여부: 구현 완료. `sources` 배열 기반으로 최대 10개까지 추가 가능하며, 10개 도달 시 추가 버튼이 disabled 처리됩니다.
- 다중 도착점 구현 여부: 구현 완료. `targets` 배열 기반으로 최대 10개까지 추가 가능하며, 10개 도달 시 추가 버튼이 disabled 처리됩니다.
- 향별 농도 구현 여부: 구현 완료. `src/simulation.js`가 source id별 field/buffer를 유지하고, `sampleTarget(target, sources)`가 전체 농도와 향별 농도 배열을 반환합니다.
- 팬 흡입/토출 구현 여부: 구현 완료. 서큘레이터 뒤쪽 흡입과 앞쪽 토출 이동을 시뮬레이션에 반영했고, Canvas에는 흡입 점선과 토출 바람선을 분리해 표시합니다.
- 온열/냉각 움직임 구현 여부: 구현 완료. 온열은 확산과 감쇠를 높이고 상승 파동을 표시합니다. 냉각은 확산과 감쇠를 낮추고 느린 링/잔향 영역을 표시합니다.
- 2.5D 뷰어 시각 자산 구현 여부: 구현 완료. 벽면 세로 그라데이션, 선반 라인, 향수병 실루엣, 바닥 그림자, 클릭 마커를 추가했습니다. `src/assets`에 로컬 SVG 자산도 추가했습니다.
- 실행한 검증 명령:
  - `node --check src\floorplan.js`
  - `node --check src\simulation.js`
  - `node --check src\app.js`
  - Node smoke test: `isWalkable`, `getWallLines`, `createSimulation`, `sampleTarget` 검증
  - `python -m http.server 4173`
  - Chrome headless DevTools 검증: `#simulator` 로딩, 출발점 10개, 도착점 10개, 향별 막대 100개, 추가 버튼 disabled, 런타임 예외 없음
  - Chrome headless DevTools 검증: `#viewer` 로딩 및 드래그 후 캔버스 변화 확인
- 남은 문제: gstack browse 바이너리는 내장 서버 시작이 15초 안에 완료되지 않아 사용하지 못했습니다. 대신 로컬 Chrome headless와 DevTools 프로토콜로 브라우저 검증을 완료했습니다.
