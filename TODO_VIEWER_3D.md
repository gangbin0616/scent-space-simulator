# 360도 뷰어 개선 TODO

## 목표

현재 2.5D raycast 뷰어를 유지하되, 사용자가 게임처럼 공간 안을 걸어 다니며 더 3D에 가까운 공간감을 느끼게 만든다.

## 작업 순서

1. 도면 좌표와 viewer 체감 공간 분리 점검
   - `src/floorplan.js` 도면 데이터는 유지한다.
   - 입구, 통로, 메인 스테이지가 좁아 보이는 원인은 도면 수정이 아니라 FOV, 벽 높이 투영, 카메라 위치, 바닥 perspective에서 먼저 찾는다.

2. WASD 이동 구현
   - `W`: 전진
   - `S`: 후진
   - `A`: 왼쪽 strafing
   - `D`: 오른쪽 strafing
   - 마우스 드래그: 시야 회전 유지
   - 벽 충돌은 `isWalkable()` 기준으로 처리한다.

3. 360도 뷰어 공간감 개선
   - 벽 높이를 과하게 키우지 않는다.
   - FOV를 넓혀 입구/통로/메인 스테이지가 답답하게 보이지 않게 한다.
   - 바닥 perspective 라인과 vignette를 개선한다.
   - 벽면은 단색 stripe가 아니라 패널, 베이스보드, 은은한 소재 레이어로 보이게 한다.

4. 3D 느낌 강화
   - ray hit 거리뿐 아니라 벽 방향을 이용해 면마다 미묘한 조명 차이를 준다.
   - 오브젝트 sprite는 거리순, 가림순으로 렌더링한다.
   - 미니맵에는 현재 위치와 이동 방향을 계속 표시한다.

5. 검증
   - `node --check src\app.js`
   - `node --check src\simulation.js`
   - 브라우저에서 `#viewer` 로딩 확인
   - WASD 이동 후 카메라 좌표가 바뀌는지 확인
   - 벽 안으로 들어가지 않는지 확인
   - 드래그 회전이 계속 작동하는지 확인
