# 도면 좌표 명세

이 문서는 향수 팝업 스토어 시뮬레이터가 원본 도면을 계속 잘못 해석하는 문제를 막기 위한 기준 문서입니다.

기준 이미지:

- 사용자가 제공한 1번 도면 이미지
- 기준 좌표계: `744 x 960`
- 원점: 이미지 좌상단
- x축: 오른쪽으로 증가
- y축: 아래쪽으로 증가

## 1. 핵심 해석 원칙

- 검은 선은 모두 벽 또는 고정 구조물입니다.
- 향은 검은 선을 통과할 수 없습니다.
- 흰 영역 중 외곽 다각형 내부만 이동/확산 가능 공간입니다.
- 하단 우측 방은 별도 방이지만, 왼쪽 세로 벽의 하단에 열린 틈이 있으므로 완전히 막힌 방으로 처리하면 안 됩니다.
- 좌측 곡선 구조물은 동심원 3개가 아닙니다.
- 좌측 곡선 구조물은 굵은 주 곡선 벽 1개와 짧은 분절/보강 벽 여러 개로 해석합니다.

## 2. 외곽 형태

외곽 이동 가능 영역은 다음 L자형 다각형으로 처리합니다.

```txt
(210, 7)
  -> (480, 7)
  -> (480, 725)
  -> (680, 725)
  -> (680, 949)
  -> (50, 949)
  -> (50, 307)
  -> (210, 307)
  -> close
```

의미:

- 상단 방은 x `210~480`, y `7~307`에 위치합니다.
- 좌측 큰 공간은 x `50~480`, y `307~949`에 위치합니다.
- 우측 하단 방은 x `480~680`, y `725~949`에 위치합니다.

## 3. 고정 직선 벽

아래 좌표는 선분의 시작점과 끝점입니다.

```txt
상단 좌측 세로벽:       (210, 307) -> (210, 7)
상단 외벽:             (210, 7)   -> (480, 7)
우측 긴 세로벽:         (480, 7)   -> (480, 826)

상단 내부 L벽 세로:     (318, 188) -> (318, 36)
상단 내부 L벽 가로:     (318, 36)  -> (470, 36)
상단 내부 짧은 가로:    (319, 191) -> (395, 191)

좌측 외벽:             (50, 949)  -> (50, 307)
좌측 상단 가로벽:       (50, 307)  -> (356, 307)
하단 외벽:             (50, 949)  -> (680, 949)

우측 하단 방 오른벽:    (680, 949) -> (680, 725)
우측 하단 방 상단벽:    (480, 725) -> (680, 725)

중앙 내부 가로벽:       (276, 413) -> (364, 413)
중앙 내부 세로벽:       (360, 413) -> (360, 902)

우측 긴 세로벽 돌출 1:  (480, 186) -> (480, 222)
우측 긴 세로벽 돌출 2:  (480, 286) -> (480, 318)
```

## 4. 하단 두꺼운 블록

하단에는 직선 외벽 외에 두껍게 보이는 검은 블록이 있습니다.

```txt
좌측 하단 블록:        x=58,  y=916, w=24, h=33
우측 방 왼쪽 블록:     x=472, y=914, w=58, h=35
우측 방 오른쪽 블록:   x=620, y=914, w=60, h=35
```

주의:

- `x=472, y=914` 블록은 우측 하단 방의 출입 틈을 완전히 막는 세로벽이 아닙니다.
- 이전 버전처럼 `(478, 825) -> (478, 949)` 세로벽을 추가하면 우측 하단 방이 잘못 막힙니다.

## 5. 좌측 곡선 벽

원본 도면의 좌측 곡선 벽은 동심원 3개가 아닙니다. 아래처럼 처리합니다.

```txt
주 곡선 벽:
  중심: (263, 716)
  반지름: 191
  시작각: 2.99 rad
  끝각: 4.63 rad
  두께: 17

하단 짧은 곡선:
  중심: (252, 716)
  반지름: 158
  시작각: 3.18 rad
  끝각: 3.38 rad
  두께: 8

중간 짧은 곡선:
  중심: (252, 716)
  반지름: 158
  시작각: 3.62 rad
  끝각: 3.82 rad
  두께: 9

상단 짧은 곡선:
  중심: (252, 716)
  반지름: 158
  시작각: 4.08 rad
  끝각: 4.28 rad
  두께: 9

상단 캡:
  중심: (263, 716)
  반지름: 191
  시작각: 4.55 rad
  끝각: 4.65 rad
  두께: 18
```

## 6. 구현 파일 기준

이 문서의 좌표는 아래 파일에 반영되어야 합니다.

- `src/floorplan.js`
- `src/simulation.js`
- `src/app.js`

`src/floorplan.js`가 단일 진실 공급원입니다.

시뮬레이션과 360도 뷰어는 둘 다 `src/floorplan.js`의 같은 벽 데이터를 사용해야 합니다. 둘 중 하나가 별도 도면을 갖게 되면 다시 도면 불일치가 발생합니다.

## 7. 검증 체크리스트

- 상단 방 좌측 세로벽이 x=210에 있어야 합니다.
- 우측 긴 세로벽은 x=480에서 y=7부터 y=826까지 내려와야 합니다.
- 우측 하단 방은 x=480~680, y=725~949 영역이어야 합니다.
- 중앙 내부 세로벽은 x=360에서 y=413~902에 있어야 합니다.
- 좌측 곡선 벽은 3개의 큰 동심원이 아니라 주 곡선 1개와 짧은 곡선 조각이어야 합니다.
- 향 농도는 고정 벽, 곡선 벽, 하단 블록, 사용자가 둔 가벽을 통과하면 안 됩니다.
